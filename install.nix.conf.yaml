- defaults:
    link:
      relink: true
      create: true
      force: true

- clean: [ '~' ]

- link:
    # xmonad & xmobar
    ~/.xmobarrc: xmobar/xmobarrc
    ~/.xmonad/xmonad.hs: xmonad/xmonad.hs
